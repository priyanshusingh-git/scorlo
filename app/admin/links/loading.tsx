import { AdminPageSkeleton } from "@/components/admin-page-skeleton";

export default function Loading() {
  return (
    <AdminPageSkeleton
      eyebrow="Link moderation"
      title="Links & Requests"
      variant="list"
    />
  );
}
